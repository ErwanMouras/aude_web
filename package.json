{
  "name": "astro-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:production": "astro build && npx pagefind --site dist --glob \"**/*.html\"",
    "build:search": "npx pagefind --site dist --glob \"**/*.html\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@getbrevo/brevo": "^3.0.1",
    "@netlify/functions": "^2.8.2",
    "@supabase/supabase-js": "^2.55.0",
    "@tailwindcss/vite": "^4.1.11",
    "astro": "^5.12.9",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
